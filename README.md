# PasswordManager
Secure Password Manager built with Python and Tkinter. Features master-password authentication, encrypted credential storage using Fernet encryption, password generation, and secure add, retrieve, search, and delete operations with local JSON-based storage.
